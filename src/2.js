// Random Node.js code generated by AI assistant

const express = require('express');
const app = express();
const port = 3000;

app.get('/', (req, res) => {
  res.send(`Hello World! ${new Date().toLocaleTimeString()}`);
});

app.listen(port, () => {
  console.log(`Server started on port ${port}`);
});
